"""Layer-specific code generators (placeholder for v2.x).

Future home of:

* ``frontend.py``    — HTML/JS/React bundles
* ``backend.py``     — FastAPI/Express servers
* ``database.py``    — SQL schemas + ORM models
* ``tests.py``       — pytest/jest scaffolds
* ``docs.py``        — README + API reference
* ``deployment.py``  — Dockerfiles, docker-compose, GitHub Actions

v0.1 only exposes the Architect's plan; no layer files are written yet.
"""
